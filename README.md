# live-entity-verification-sdk
Cross-signal API that verifies whether a domain/entity an LLM claims exists actually does, before that claim gets acted on -- fuses WHOIS/RDAP, Certificate Transparency, Wayback Machine, and DNS maturity into one fail-closed Bayesian verdict.
